import { createSlotFill } from '@wordpress/components';

const { Fill, Slot } = createSlotFill('CosteredBlockControls');
export const CosteredBlockControls = { Fill, Slot };
export default CosteredBlockControls;